# ws development

`bin/ws` is the canonical workspace CLI. Keep it dependency-free, readable, idempotent, and conservative around Git state. Do not add lock-based reconstruction unless explicitly requested.
