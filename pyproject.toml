[project]
name = "wsforge"
version = "1.0.0"
description = "Recursive workspace forking CLI"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ws = "wsforge.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
